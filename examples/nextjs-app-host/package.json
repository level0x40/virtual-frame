{
  "name": "example-nextjs-app-host",
  "private": true,
  "scripts": {
    "dev": "PORT=${PORT:-3000} next dev",
    "build": "next build",
    "start": "PORT=${PORT:-3000} next start",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@virtual-frame/next": "workspace:*",
    "@virtual-frame/react": "workspace:*",
    "@virtual-frame/store": "workspace:*",
    "next": "catalog:",
    "react": "catalog:",
    "react-dom": "catalog:",
    "virtual-frame": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "typescript": "catalog:"
  }
}
