/**
 * @virtual-frame/sveltekit/client — Client-only entry.
 *
 * Exports the VirtualFrame Svelte component with SSR HTML support.
 */

export { default as VirtualFrame } from "./VirtualFrameSSR.svelte";
